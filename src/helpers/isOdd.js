// Helper to see if shipment's destination street name is odd or even.
// Not really necesary but helps code readability.

const isOdd = (number) => {
     return !(number % 2);
};

export default isOdd;